

		/**
		 * @return bool
		 */
		public function deĺete(){
			if($this->getId()){
				$this->where('id=?', $this->id)->delete();
				{unsetCommands}
				return true;
			} else {
				return false;
			}
		}
